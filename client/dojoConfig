import { createDojoConfig } from "@dojoengine/core";

import manifest from "../dojo-starter/manifest_dev.json";

export const dojoConfig = createDojoConfig({
    manifest,
    toriiUrl: "https://api.cartridge.gg/x/testBrentimus/torii",
});
